/**
 * @module grid-context
 * @lang zh-CN UGrid 与 UGridItem 之间的私有布局 context；不承载业务、身份或网络状态。
 * @lang en Private layout context between UGrid and UGridItem; carries no business, identity, or network state.
 */
export const GRID_CONTEXT = Symbol('u-grid-context');
