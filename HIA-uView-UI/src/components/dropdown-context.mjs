/**
 * @module dropdown-context
 * @lang zh-CN UDropdown 与 UDropdownItem 之间的私有局部选择 context，不承载业务或路由状态。
 * @lang en Private local selection context between UDropdown and UDropdownItem; it carries no business or route state.
 */
export const DROPDOWN_CONTEXT = Symbol('u-dropdown-context');
