/**
 * @module hia-uview-ui-global-components-runtime
 * @lang zh-CN 作为可选类型入口的零副作用 runtime 占位模块。它存在只为让 package export 在 JavaScript resolver 中保持有效；不会注册组件或写入全局状态。
 * @lang en Zero-side-effect runtime placeholder module for the optional type entry. It exists only to keep the package export valid for JavaScript resolvers; it neither registers components nor writes global state.
 */

export {};
